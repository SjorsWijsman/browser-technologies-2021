<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
  <%- include('partials/head') %>
</head>
<body>
  <h1>Your Shirts</h1>
  <% if (locals.userData) { %>
    <ol>
      <% for ([i, shirt] of Object.keys(userData.shirts).entries()) { %>
        <li>
          <a href="../?color=<%= userData.shirts[shirt].color %>&size=<%= userData.shirts[shirt].size %>&sex=<%= userData.shirts[shirt].sex %>">
          <% for (setting of Object.keys(shirtData)) { %>
            <%= userData.shirts[shirt][setting] %>
          <% } %>
          </a>
          <button type="button" name="button">Delete</button>
        </li>
      <% } %>
    </ol>
  <% } else { %>
    <p>You don't have any shirts saved. Start creating your first shirt <a href="../">here</a>!</p>
  <% } %>
</body>
</html>
